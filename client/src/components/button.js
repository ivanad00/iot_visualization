import React from "react";
import "../app.css";

export const Button = () => {
  const onClick = () => {};

  return (
    <button onClick={onClick} className="button">
      Choose device
    </button>
  );
};
